/**
 * 🌸 IllustrationEngine
 * Returns null to ensure no synthetic SVG vector figures override full-res jaga.png background artwork.
 */
export class IllustrationEngine {
  resolve() {
    return null;
  }
}
